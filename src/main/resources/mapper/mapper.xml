<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="myproject.whatproject.mapper.MyMapper">

    <select id="findAllUsers" resultType="myproject.whatproject.domain.user.User">
        SELECT * FROM USER
    </select>

    <insert id="saveUser" parameterType="myproject.whatproject.domain.user.User">
        INSERT INTO USER (email, id, pw, name, contact, dob, gender, grade)
        VALUES (#{email}, #{id}, #{pw}, #{name}, #{contact}, #{dob}, #{gender}, #{grade})
    </insert>

</mapper>
